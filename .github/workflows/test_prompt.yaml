name: test-prompt

on: 
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test-prompt:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Set up Pixi
        uses: prefix-dev/setup-pixi@v0.9.1
        with:
          environments: test
          frozen: true

      # Test
      - name: Test
        run: |
          pixi run --frozen -e test pytest -v tests/ -k "prompt"
