name: test-unit-mechanalyzer

on: 
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test-unit-mechanalyzer:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Set up Pixi
        uses: prefix-dev/setup-pixi@v0.8.9

      # Test
      - name: Test
        run: |
          pixi run -e test pytest -v src/_mechanalyzer/ratefit
          pixi run -e test pytest -v src/_mechanalyzer/mechanalyzer
          pixi run -e test pytest -v src/_mechanalyzer/examples
