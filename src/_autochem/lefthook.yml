pre-commit:
  parallel: true
  commands:
    ruff-linter:
      run: ruff check --select I --fix
    run-format:
      run: ruff format --check

pre-push:
  commands:
    # old:
    pylint-linter:
      run: pylint --rcfile=.pylintrc automol
