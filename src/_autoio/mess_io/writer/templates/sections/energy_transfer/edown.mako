EnergyRelaxation
  Exponential
     Factor[1/cm]                     ${exp_factor}
     Power                            ${exp_power}
     ExponentCutoff                   ${exp_cutoff}
End  ! EneRelax
